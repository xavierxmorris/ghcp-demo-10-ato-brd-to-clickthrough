# Verification tooling only - the demo itself needs none of this.
node_modules/
package-lock.json

# Scratch output from testing/smoke.mjs
testing/shots/

# Scratch output of .\go.ps1 -Live. The committed copy of one such run is
# prototype-rebuild/, kept so the --target result is reproducible without a
# 22-minute rebuild.
prototype-live/

# OS and editor noise
Thumbs.db
.DS_Store
.vscode/
